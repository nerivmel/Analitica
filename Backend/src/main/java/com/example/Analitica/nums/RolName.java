package com.example.Analitica.nums;

public enum RolName {

    //El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(ver solo las graficas sin necesidad de estar logeado)

    ROLE_ADMIN,ROLE_USER
}